# AGENTS.md — Second Brain

> Generated from `CLAUDE.md` by `install.sh --agent=codex`.
> Codex CLI reads this file at session start. Skills are installed separately under `~/.codex/skills/<prefix>-<name>` and mirrored in the target vault under `.codex/skills/`.
