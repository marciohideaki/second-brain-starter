# Second Brain — Global Instructions

> This block is added to `~/.claude/CLAUDE.md` by `install.sh`.
> To apply manually, copy the "## Second Brain" section below to the end of your `~/.claude/CLAUDE.md`.

---

## Second Brain

Vault active at `{VAULT}`.

### When starting any session

1. Read `{VAULT}/_memory/current-state.md` — recent context.
2. If the session involves the vault directly (any `/` skill or knowledge work), also read `{VAULT}/CLAUDE.md`.

### Available skills

`/init`, `/braindump`, `/ingest`, `/wiki-build`, `/focus`, `/daily-briefing`, `/weekly-review`, `/end-session`, `/session-handoff`, `/content-idea`, `/lint`, `/skill-improve`.

---

### Portuguese (BR) — instruções resumidas

Vault ativo em `{VAULT}`.

Ao iniciar qualquer sessão: leia `{VAULT}/_memory/current-state.md` (sempre) e, se a sessão envolver o vault, leia também `{VAULT}/CLAUDE.md`.
